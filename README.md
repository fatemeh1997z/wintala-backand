# Win Tala - Frontend (React + Vite + Tailwind)

This is a starter frontend for the *وین طلا* project.

## Run locally
1. `npm install`
2. `npm run dev`

Set `REACT_APP_API_BASE` environment variable or edit `axios.defaults.baseURL` in `src/App.jsx`.
